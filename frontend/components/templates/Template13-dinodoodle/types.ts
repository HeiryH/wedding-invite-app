export type {
  Breakpoint,
  LayerKind,
  ObjectFit,
  Layer,
  StageDef,
  StageLayout,
  SlotProps,
  EditorHandle,
} from '@/components/templates/_shared/types';

export type StageId = 'welcome' | 'details' | 'itinerary' | 'rsvp' | 'wishes' | 'photobooth';
